# Changelog

This project is currently pre-alpha at version `0.1.0`.

Track notable user-facing changes here before each release. Include package
changes for both `pyfend` and `pyfend-cli` when they are released together.
